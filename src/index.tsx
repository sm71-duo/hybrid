import App from './App';
import React from 'react';
import {  View } from 'react-native';

const Root = (): JSX.Element => {
    return (
      <View>
          <App />
      </View>
    );
};

export default Root;
