import React from 'react';
import { View } from 'react-native';
import AudioView from 'Views/AudioView';

const App = () => {
    return (
        <View>
            <AudioView />
        </View>
    );
};

export default App;
